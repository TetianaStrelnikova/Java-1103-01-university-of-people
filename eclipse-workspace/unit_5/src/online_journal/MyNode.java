package online_journal;

/**
 * Represents a node in the linked list used in the hash table.
 * A ListNode holds a (key,value) pair.
 */
class MyNode {
	   String key;
	   String value;
	   MyNode next;
	}
